{
  "name": "oss-wishlist-analyzer-action",
  "version": "1.0.0",
  "description": "GitHub Action to analyze OSS dependencies using SBOM and ecosyste.ms",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build index.js -o dist --source-map --license licenses.txt",
    "test": "jest"
  },
  "keywords": [
    "github-action",
    "sbom",
    "security",
    "dependencies",
    "supply-chain"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3",
    "jest": "^29.7.0"
  }
}
